import 'package:flutter/material.dart';

class ContactScreen extends StatelessWidget {
  const ContactScreen();

  @override
  Widget build(BuildContext context) {
    return Text("DANH BẠ", style: const TextStyle(fontSize: 28));
  }
}
