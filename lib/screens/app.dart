import 'package:flutter/material.dart';

class AppScreen extends StatelessWidget {
  const AppScreen();

  @override
  Widget build(BuildContext context) {
    return Text("ỨNG DỤNG", style: const TextStyle(fontSize: 28));
  }
}
