import 'package:flutter/material.dart';

class CalenderScreen extends StatelessWidget {
  const CalenderScreen();

  @override
  Widget build(BuildContext context) {
    return Text("LỊCH", style: const TextStyle(fontSize: 28));
  }
}
